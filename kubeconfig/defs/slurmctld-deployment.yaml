apiVersion: apps/v1
kind: Deployment
metadata:
  name: slurmctld-deployment
  labels:
    app: slurmctld
spec:
  replicas: 1
  selector:
    matchLabels:
      app: slurmctld
  template:
    metadata:
      labels:
        app: slurmctld
    spec:
      hostname: slurm
      hostAliases:
      - ip: "192.168.100.101"
        hostnames:
        - "node01"
      containers:
      #
      # The slurmctld container
      #
      - name: slurmctld
        image: 192.168.100.2:5000/slurmctld
        #command: ['/sbin/init']
        ports:
        - containerPort: 6817
        volumeMounts:
        - name: slurm-conf
          mountPath: "/etc/slurm/"
          readOnly: true
        - name: run-munge
          mountPath: "/run/munge"
      #
      # The munge container
      #
      - name: munge
        image: 192.168.100.2:5000/munge
        #command: ['/sbin/init']
        volumeMounts:
        - name: munge-key
          mountPath: "/data/munge/"
          readOnly: true
        - name: run-munge
          mountPath: "/run/munge"
      volumes:
      - name: slurm-conf
        configMap:
          name: slurm-conf
      - name: munge-key
        secret:
          secretName: munge-key
          defaultMode: 0400
      - name: run-munge
        emptyDir: {}
